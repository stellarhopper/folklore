# MQTT credentials for deployment listener
# Copy this file to .env.mqtt and fill in your own values (chmod 600).
# Never commit .env.mqtt - it is gitignored.
MQTT_BROKER=your.broker.hostname
MQTT_USERNAME=your_username
MQTT_PASSWORD=your_password_here
